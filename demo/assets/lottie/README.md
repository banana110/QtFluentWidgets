# Demo Lottie assets

These JSON files are embedded into `QtFluentDemo` through `demo/lottie_assets.qrc`.
They come from the curated local set used for the demo, and are intentionally
kept small and icon-like so they can be tinted by `FluentLottieWidget` in
buttons, navigation items, cards, and theme-linkage examples.
